import NotificationsScreen from '@/app/notifications';

/** Notifications as a tab, in the Android look (see _layout.tsx). */
export default function ActivityTab() {
  return <NotificationsScreen tab />;
}
