import { M3Explore } from '@/components/m3/screens/explore';

/** Search, trends and videos: a tab in the Android look (see _layout.tsx). */
export default function ExploreTab() {
  return <M3Explore />;
}
